// Determine whether a number is even or odd using the ternary operator.

const value = 26;
// if (value%2==0) console.log('number is even');
// else console.log('number is odd');
console.log( value%2 == 0 ? 'number is even': 'number is odd');